import Link from "next/link"; import {modules} from "../lib/modules";
const stats=[["Organizations","0","Ready for setup"],["Users","0","Access accounts"],["Active modules",String(modules.length),"Available modules"],["Branches","0","Locations"]];
export default function Home(){return <div className="app-shell"><aside className="sidebar"><div className="brand">SUK Management System<span>MODULAR MANAGEMENT PLATFORM</span></div><nav className="nav">{["Dashboard","Organizations","People","Finance","Inventory","Reports","Settings"].map((x,i)=><Link className={i===0?"active":""} href={i===0?"/":"/dashboard"} key={x}>{x}</Link>)}</nav></aside><main className="main"><div className="topbar"><div><div className="eyebrow">Management platform</div><h1 className="title">Welcome to SUK.</h1><div className="subtitle">One system for schools, offices, restaurants and other organizations.</div></div><Link className="button" href="/onboarding">Set up organization</Link></div><div className="grid">{stats.map(([label,value,note])=><div className="card" key={label}><div className="muted">{label}</div><div className="metric">{value}</div><div className="muted">{note}</div></div>)}</div><section className="section"><div className="section-title">Platform modules</div><div className="modules">{modules.map(m=><div className="module" key={m.id}><h3>{m.name}</h3><p>{m.description}</p></div>)}</div></section><section className="section"><div className="card"><div className="section-title">Built for growth</div><p className="subtitle">Organizations activate only the modules they need while sharing secure core services for users, finance, inventory, reporting, documents and notifications.</p></div></section></main></div>}